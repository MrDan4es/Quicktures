export default interface IImageData {
  title: string;
  url: string;
  id: number;
  date_create: string;
  username: string;
}

export interface IPostImageData {
  title: string;
  url: string;
}

export interface IUsername {
  username: string;
}
